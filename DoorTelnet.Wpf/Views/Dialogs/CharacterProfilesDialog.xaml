<Window x:Class="DoorTelnet.Wpf.Views.Dialogs.CharacterProfilesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Character Profiles" Height="420" Width="520" WindowStartupLocation="CenterOwner"
        Background="{StaticResource Brush.Panel}" Foreground="{StaticResource Brush.Text}" WindowStyle="ToolWindow">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Character profiles" FontWeight="Bold" FontSize="14" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,6">
            <TextBlock Text="User:" VerticalAlignment="Center" Margin="0,0,6,0" />
            <TextBox Width="160" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Width="80" Margin="10,0,0,0" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Characters}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                  Background="#202020" BorderBrush="#444" Foreground="{StaticResource Brush.Text}" HeadersVisibility="Column" RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Class" Binding="{Binding Class}" Width="100" />
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="60" />
                <DataGridTextColumn Header="XP" Binding="{Binding Experience}" Width="120" />
                <DataGridTextColumn Header="House" Binding="{Binding House}" Width="100" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Close" Width="90" Command="{Binding CloseCommand}" />
        </StackPanel>
    </Grid>
</Window>