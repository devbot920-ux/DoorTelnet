<Window x:Class="DoorTelnet.Wpf.Views.Dialogs.CharacterSheetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Character Sheet" Height="600" Width="800" Background="{StaticResource Brush.Background}" Foreground="{StaticResource Brush.Text}" WindowStartupLocation="CenterOwner">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock FontSize="20" FontWeight="Bold" Text="Character" Margin="0,0,0,8" />
                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Name" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Name}"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Class" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Class}"/>
                    </StackPanel>
                </StackPanel>

                <GroupBox Header="Automation" Margin="0,0,0,12">
                    <StackPanel Margin="6" Orientation="Vertical">
                        <WrapPanel>
                            <CheckBox Content="Auto Shield" IsChecked="{Binding AutoShield}" Margin="4"/>
                            <CheckBox Content="Auto Heal" IsChecked="{Binding AutoHeal}" Margin="4"/>
                            <CheckBox Content="Auto Gong" IsChecked="{Binding AutoGong}" Margin="4"/>
                            <CheckBox Content="Pickup Gold" IsChecked="{Binding PickupGold}" Margin="4"/>
                            <CheckBox Content="Pickup Silver" IsChecked="{Binding PickupSilver}" Margin="4"/>
                        </WrapPanel>
                        <UniformGrid Columns="3" Margin="4,8,4,0">
                            <StackPanel Margin="4">
                                <TextBlock Text="Shield Refresh (sec)" FontSize="11"/>
                                <TextBox Text="{Binding ShieldRefreshSec, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Gong HP %" FontSize="11"/>
                                <TextBox Text="{Binding GongMinHpPercent, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                            </StackPanel>
                            <StackPanel Margin="4">
                                <TextBlock Text="Auto Heal HP %" FontSize="11"/>
                                <TextBox Text="{Binding AutoHealHpPercent, UpdateSourceTrigger=PropertyChanged}" Width="80" />
                            </StackPanel>
                        </UniformGrid>
                    </StackPanel>
                </GroupBox>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Heals" FontWeight="Bold" Grid.Column="0"/>
                    <TextBlock Text="Shields" FontWeight="Bold" Grid.Column="1"/>
                    <TextBlock Text="Inventory" FontWeight="Bold" Grid.Column="2"/>

                    <ListBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Heals}" Height="120">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="2">
                                    <TextBlock Text="{Binding Short}" FontWeight="Bold" Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding Spell}" Margin="0,0,6,0"/>
                                    <TextBlock Text="x" Margin="0,0,4,0"/>
                                    <TextBlock Text="{Binding Heals}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <ListBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Shields}" Height="120"/>
                    <ListBox Grid.Row="1" Grid.Column="2" ItemsSource="{Binding Inventory}" Height="120"/>
                </Grid>

                <GroupBox Header="Spells" Margin="0,0,0,12">
                    <DataGrid ItemsSource="{Binding Spells}" AutoGenerateColumns="False" CanUserAddRows="False" Height="200" Background="{StaticResource Brush.Panel}" Foreground="{StaticResource Brush.Text}" BorderBrush="{StaticResource Brush.Border}" RowBackground="{StaticResource Brush.Panel}" AlternatingRowBackground="#262626">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Nick" Binding="{Binding Nick}" Width="60"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding LongName}" Width="*"/>
                            <DataGridTextColumn Header="Sphere" Binding="{Binding Sphere}" Width="80"/>
                            <DataGridTextColumn Header="Mana" Binding="{Binding Mana}" Width="60"/>
                            <DataGridTextColumn Header="Diff" Binding="{Binding Diff}" Width="60"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </GroupBox>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
                    <Button Content="Close" Command="{Binding CloseCommand}" Width="90"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>