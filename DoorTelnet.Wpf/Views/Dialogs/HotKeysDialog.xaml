<Window x:Class="DoorTelnet.Wpf.Views.Dialogs.HotKeysDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hot Keys" Height="500" Width="600" Background="{StaticResource Brush.Background}" 
        Foreground="{StaticResource Brush.Text}" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Hot Keys Configuration" FontWeight="Bold" FontSize="14" Grid.Row="0" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding HotKeys}" 
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" 
                  RowHeaderWidth="0" FontSize="12" HeadersVisibility="Column"
                  Background="{StaticResource Brush.Panel}" Foreground="{StaticResource Brush.Text}" 
                  BorderBrush="{StaticResource Brush.Border}" RowBackground="#262626" 
                  AlternatingRowBackground="#303030">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Key" Binding="{Binding Key}" Width="80" IsReadOnly="True"/>
                <DataGridTextColumn Header="Script" Binding="{Binding Script, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Reload" Width="80" Command="{Binding ReloadCommand}" Margin="0,0,6,0"/>
            <Button Content="Save" Width="80" Command="{Binding SaveCommand}" Margin="0,0,6,0"/>
            <Button Content="Close" Width="80" Command="{Binding CloseCommand}"/>
        </StackPanel>
    </Grid>
</Window>