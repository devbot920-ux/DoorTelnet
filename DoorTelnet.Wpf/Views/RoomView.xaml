<UserControl x:Class="DoorTelnet.Wpf.Views.RoomView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" MinWidth="260" MinHeight="240">
    <StackPanel>
        <TextBlock Text="Room" FontWeight="Bold" FontSize="14" />
        <TextBlock Text="{Binding RoomName}" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0 2 0 6" />
        
        <!-- Room ID -->
        <TextBlock Text="Room ID" FontWeight="Bold" FontSize="11" Margin="0 0 0 2" />
        <TextBlock Text="{Binding RoomId}" FontSize="10" Foreground="#CCC" Margin="0 0 0 6" />

        <TextBlock Text="Exits" FontWeight="Bold" />
        <ItemsControl ItemsSource="{Binding Exits}" Margin="0 2 0 6">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="#2d2d2d" BorderBrush="#555" BorderThickness="1" CornerRadius="4" Padding="6,2" Margin="0 0 6 6">
                        <TextBlock Text="{Binding}" FontSize="11" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Text="Monsters" FontWeight="Bold" />
        <ItemsControl ItemsSource="{Binding Monsters}" Margin="0 2 0 8">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DisplayName}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="White" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAggressive}" Value="True">
                                        <Setter Property="Foreground" Value="#FFFF0000" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding TargetingYou}" Value="True">
                                        <Setter Property="FontWeight" Value="Bold" />
                                        <Setter Property="Foreground" Value="#FFFF0000" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <TextBlock Text="Items" FontWeight="Bold" />
        <ItemsControl ItemsSource="{Binding Items}" Margin="0 2 0 8">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Navigation Section (moved to bottom) -->
        <TextBlock Text="Navigation" FontWeight="Bold" Margin="0 8 0 2" />
        <Grid Margin="0 2 0 6">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Movement Mode Selector -->
            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" 
                       Text="Movement Mode:" FontWeight="SemiBold" FontSize="10" 
                       Margin="0 0 0 2" Foreground="#CCC"/>
            
            <ComboBox Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                      ItemsSource="{Binding MovementModes}"
                      SelectedItem="{Binding SelectedMovementMode}"
                      FontSize="10" Margin="0 0 0 4"
                      ToolTip="Select movement speed vs reliability">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Icon}" FontSize="12" Margin="0 0 6 0" VerticalAlignment="Center"/>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="10"/>
                                <TextBlock Text="{Binding Description}" FontSize="9" Foreground="#888"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            
            <!-- Quick mode selection buttons -->
            <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" 
                        Orientation="Horizontal" Margin="0 2 0 6">
                <Button Content="? Ultra" 
                        Command="{Binding SelectUltraFastModeCommand}"
                        FontSize="8" Padding="4,1" Margin="0 0 2 0"
                        ToolTip="Ultra-Fast: 50ms delays (paste-friendly)"
                        Background="#4a148c" BorderBrush="#6a1b9a"/>
                <Button Content="?? Fast" 
                        Command="{Binding SelectFastModeCommand}"
                        FontSize="8" Padding="4,1" Margin="0 0 2 0"
                        ToolTip="Fast: 200ms delays (safe areas)"
                        Background="#1565c0" BorderBrush="#1976d2"/>
                <Button Content="??? Safe" 
                        Command="{Binding SelectReliableModeCommand}"
                        FontSize="8" Padding="4,1"
                        ToolTip="Reliable: Wait for room detection"
                        Background="#2e7d32" BorderBrush="#388e3c"/>
            </StackPanel>
            
            <!-- Autocomplete Navigation ComboBox -->
            <ComboBox Grid.Row="3" Grid.Column="0" 
                      Text="{Binding NavigationDestination, UpdateSourceTrigger=PropertyChanged}" 
                      ItemsSource="{Binding NavigationSuggestions}"
                      SelectedItem="{Binding SelectedSuggestion}"
                      IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                      IsEditable="True"
                      IsTextSearchEnabled="False"
                      FontSize="11" Margin="0 0 4 2"
                      ToolTip="Enter room name, room ID, or 'store' to find nearby stores"
                      Loaded="ComboBox_Loaded">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4,2">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Main suggestion text -->
                            <TextBlock Grid.Row="0" 
                                       Text="{Binding DisplayText}" 
                                       FontSize="11" 
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource Brush.Text}"/>
                            
                            <!-- Room ID subtitle -->
                            <TextBlock Grid.Row="1" 
                                       Text="{Binding RoomId, StringFormat='Room ID: {0}'}" 
                                       FontSize="9" 
                                       Foreground="{StaticResource Brush.TextMuted}" 
                                       Margin="0,1,0,0"/>
                        </Grid>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            
            <!-- Navigate button -->
            <Button Grid.Row="3" Grid.Column="1" 
                    Content="Go" 
                    Command="{Binding StartNavigationCommand}"
                    FontSize="10" Padding="8,2" Margin="2 0"/>
            
            <!-- Stop button -->
            <Button Grid.Row="3" Grid.Column="2" 
                    Content="Stop" 
                    Command="{Binding StopNavigationCommand}"
                    FontSize="10" Padding="8,2" Margin="2 0 0 0"/>
            
            <!-- Quick actions row - Fixed spacing and layout -->
            <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" 
                        Orientation="Horizontal" Margin="0 4 0 8">
                <Button Content="Find Stores" 
                        Command="{Binding FindStoresCommand}"
                        FontSize="9" Padding="6,2" Margin="0 0 4 0"
                        ToolTip="Find nearby stores within 40 moves"/>
                <Button Content="Set Pending" 
                        Command="{Binding SetPendingDestinationCommand}"
                        FontSize="9" Padding="6,2"
                        ToolTip="Navigate when safe"/>
            </StackPanel>
            
            <!-- Status display - Moved to separate row with more top margin -->
            <Border Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3"
                    Background="#2a2a2a" BorderBrush="#444" BorderThickness="1" 
                    CornerRadius="3" Padding="6,4" Margin="0 4 0 0">
                <TextBlock Text="{Binding NavigationStatus}" 
                           FontSize="10" Foreground="#AAA" 
                           TextWrapping="Wrap"/>
            </Border>
        </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 8 0 0">
            <TextBlock Text="Updated:" FontSize="10" Foreground="#888" />
            <TextBlock Text=" " FontSize="10" />
            <TextBlock Text="{Binding LastUpdated}" FontSize="10" Foreground="#888" />
        </StackPanel>
    </StackPanel>
</UserControl>