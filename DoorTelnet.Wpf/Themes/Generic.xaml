<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DoorTelnet.Wpf.Controls">
    <!-- Updated style for TerminalControl: remove opaque border that hid drawing surface -->
    <Style TargetType="local:TerminalControl">
        <Setter Property="Background" Value="Black" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:TerminalControl">
                    <Grid>
                        <!-- ScrollBar overlays right edge; drawing happens in OnRender behind it -->
                        <ScrollBar x:Name="PART_VScroll"
                                   Orientation="Vertical"
                                   HorizontalAlignment="Right"
                                   Width="14"
                                   Margin="0"
                                   Visibility="Collapsed"
                                   Minimum="0" Maximum="1"
                                   SmallChange="1" LargeChange="5"
                                   Background="#202020" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>